# API配置
# 当前前端默认走 Next rewrites 转发到 BACKEND_URL，通常不需要显式设置 NEXT_PUBLIC_API_URL。
NEXT_PUBLIC_API_URL=
BACKEND_URL=http://localhost:3001

# SecondMe OAuth配置
NEXT_PUBLIC_SECONDME_CLIENT_ID=your_client_id_here
NEXT_PUBLIC_SECONDME_REDIRECT_URI=http://localhost:3000/auth/callback
